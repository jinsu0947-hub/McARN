# SPEC_GenerationScope — 생성 범위 (폐기됨)

**이 문서는 폐기됐다. `SPEC_Scope_v0.2.md`가 대체한다.**

이 판(`v0.1`)은 "노선 폴리라인 중심의 띠"만을 생성 단위로 두고, 그 안에 영도구·부산 508번 버스에 고유한 절단 규칙("영도 범위" 좌표 판정, 508만 예외로 전 구간 유지)을 하드코딩했다. 다른 지역이나 노선 없는 실행을 감당하지 못해, scope를 사각형·행정구역 폴리곤·노선 띠의 합집합으로 일반화하고 노선 특례를 없앤 `SPEC_Scope_v0.2.md`로 대체됐다(2026-09-18).

- 층위(L0~L3) 정의, 경계 처리, 파라미터: `SPEC_Scope_v0.2.md §2, §3, §6`
- 노선·정류소 포함 판정: `SPEC_Scope_v0.2.md §4` (이 판 §1.1의 절단 규칙과 좌표 범위 판정을 대체 — 특례 없이 scope 판정 하나로 통일됐다)
- 영도·508 관련 내용 전부 (노선 목록, 사각형 경계, 단계 P0~P2, 파라미터 실값, 청크 예산): `SPEC_Scope_v0.2.md §7` "영도 프리셋 예시"

이 파일은 이전 설계의 기록으로만 남긴다. 새 작업은 `SPEC_Scope_v0.2.md`를 본다.
